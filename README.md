Instale o pacote do graphviz:
Em sistemas baseados no Debian:
```
    sudo apt install -y graphviz-dev
```
Se não funcionar, tente:
```
    sudo apt install -y graphviz-devel
```


Fedora:
```
    sudo dnf install -y graphviz-devel
```
